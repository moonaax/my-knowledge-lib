# 回溯

## 核心技巧

- 回溯模板：选择 → 递归 → 撤销选择
- 剪枝：排序后跳过重复元素、提前终止
- 组合问题用 startIndex 避免重复，排列问题用 visited 数组
- 画递归树帮助理解

```
void backtrack(路径, 选择列表) {
    if (满足结束条件) { 收集结果; return; }
    for (选择 : 选择列表) {
        做选择;
        backtrack(路径, 选择列表);
        撤销选择;
    }
}
```

---

## 1. 全排列（#46）

```java
public List<List<Integer>> permute(int[] nums) {
    List<List<Integer>> res = new ArrayList<>();
    backtrack(nums, new boolean[nums.length], new ArrayList<>(), res);
    return res;
}

private void backtrack(int[] nums, boolean[] used, List<Integer> path, List<List<Integer>> res) {
    if (path.size() == nums.length) {
        res.add(new ArrayList<>(path));
        return;
    }
    for (int i = 0; i < nums.length; i++) {
        if (used[i]) continue;
        used[i] = true;
        path.add(nums[i]);
        backtrack(nums, used, path, res);
        path.remove(path.size() - 1);
        used[i] = false;
    }
}
```

- 时间 O(n·n!)，空间 O(n)
- 踩坑：`res.add(new ArrayList<>(path))` 必须拷贝

**📝 解析**

- **思路推导**：排列问题每个位置都要从所有元素中选，用 used 数组标记已选元素。递归树的每一层是一个位置的选择。
- **关键点**：`new ArrayList<>(path)` 深拷贝，否则 res 里存的都是同一个引用，最终全是空列表。
- **易错点**：全排列 II（#47）有重复元素时，需要排序 + `i>0 && nums[i]==nums[i-1] && !used[i-1]` 剪枝。
- **举一反三**：下一个排列（#31）→ 不用回溯，找规律直接算

---

## 2. 子集（#78）

```java
public List<List<Integer>> subsets(int[] nums) {
    List<List<Integer>> res = new ArrayList<>();
    backtrack(nums, 0, new ArrayList<>(), res);
    return res;
}

private void backtrack(int[] nums, int start, List<Integer> path, List<List<Integer>> res) {
    res.add(new ArrayList<>(path));
    for (int i = start; i < nums.length; i++) {
        path.add(nums[i]);
        backtrack(nums, i + 1, path, res);
        path.remove(path.size() - 1);
    }
}
```

- 时间 O(n·2^n)，空间 O(n)
- 每个节点都收集结果，不只是叶子

**📝 解析**

- **思路推导**：每个元素选或不选，共 2^n 种子集。回溯写法：用 startIndex 保证不回头选，每个递归节点都收集当前 path。
- **关键点**：和组合的区别——子集在每个节点收集结果，组合只在满足条件时收集。
- **易错点**：子集 II（#90）有重复元素时，排序 + `i>start && nums[i]==nums[i-1]` 跳过同层重复。
- **举一反三**：也可以用位运算枚举：0~2^n-1 每个数的二进制表示一个子集

---

## 3. 组合总和（#39）

> 数字可重复使用，找所有和为 target 的组合

```java
public List<List<Integer>> combinationSum(int[] candidates, int target) {
    List<List<Integer>> res = new ArrayList<>();
    Arrays.sort(candidates);
    backtrack(candidates, target, 0, new ArrayList<>(), res);
    return res;
}

private void backtrack(int[] nums, int remain, int start, List<Integer> path, List<List<Integer>> res) {
    if (remain == 0) { res.add(new ArrayList<>(path)); return; }
    for (int i = start; i < nums.length; i++) {
        if (nums[i] > remain) break; // 剪枝
        path.add(nums[i]);
        backtrack(nums, remain - nums[i], i, path, res); // i 不是 i+1，可重复
        path.remove(path.size() - 1);
    }
}
```

- 排序 + `nums[i] > remain` 剪枝效果显著

**📝 解析**

- **思路推导**：可重复选 → 递归时传 i 而不是 i+1。排序后 `nums[i] > remain` 直接 break，后面的更大不用试了。
- **关键点**：和组合总和 II（#40）的区别——II 每个数只能用一次（传 i+1），且需要去重。
- **易错点**：不排序也能 AC，但排序后剪枝能大幅减少递归次数。
- **举一反三**：零钱兑换（#322）→ 同样是可重复选，但求最少个数，用 DP 更合适

---

## 4. 电话号码的字母组合（#17）

```java
private static final String[] MAPPING = {"", "", "abc", "def", "ghi", "jkl", "mno", "pqrs", "tuv", "wxyz"};

public List<String> letterCombinations(String digits) {
    List<String> res = new ArrayList<>();
    if (digits.isEmpty()) return res;
    backtrack(digits, 0, new StringBuilder(), res);
    return res;
}

private void backtrack(String digits, int idx, StringBuilder sb, List<String> res) {
    if (idx == digits.length()) { res.add(sb.toString()); return; }
    for (char c : MAPPING[digits.charAt(idx) - '0'].toCharArray()) {
        sb.append(c);
        backtrack(digits, idx + 1, sb, res);
        sb.deleteCharAt(sb.length() - 1);
    }
}
```

- 时间 O(3^n × 4^m)，n 是 3 字母键数，m 是 4 字母键数

**📝 解析**

- **思路推导**：每个数字对应几个字母，逐位选择。不需要去重，不需要 startIndex，因为每一位选的是不同数字的字母。
- **关键点**：用 idx 追踪当前处理到第几个数字，每层递归处理一个数字的所有字母选项。
- **易错点**：digits 为空时直接返回空列表，不要返回包含空字符串的列表。
- **举一反三**：这是最简单的回溯题之一，适合入门理解回溯框架

---

## 5. 括号生成（#22）

> 生成 n 对有效括号的所有组合

```java
public List<String> generateParenthesis(int n) {
    List<String> res = new ArrayList<>();
    backtrack(n, 0, 0, new StringBuilder(), res);
    return res;
}

private void backtrack(int n, int open, int close, StringBuilder sb, List<String> res) {
    if (sb.length() == 2 * n) { res.add(sb.toString()); return; }
    if (open < n) {
        sb.append('(');
        backtrack(n, open + 1, close, sb, res);
        sb.deleteCharAt(sb.length() - 1);
    }
    if (close < open) {
        sb.append(')');
        backtrack(n, open, close + 1, sb, res);
        sb.deleteCharAt(sb.length() - 1);
    }
}
```

- 剪枝条件：左括号数 < n 才能加左，右括号数 < 左括号数才能加右

**📝 解析**

- **思路推导**：暴力生成所有 2n 长度的括号串再验证 O(2^2n)。回溯+剪枝：用 open/close 计数，保证任何时刻右括号不超过左括号。
- **关键点**：两个剪枝条件就保证了生成的一定是有效括号，不需要后验证。结果数是卡特兰数 C(2n,n)/(n+1)。
- **易错点**：StringBuilder 的 `deleteCharAt(sb.length()-1)` 是回溯的关键，别忘了。
- **举一反三**：不同的二叉搜索树（#96）→ 也是卡特兰数

---

## 6. 单词搜索（#79）

> 在二维网格中搜索单词

```java
public boolean exist(char[][] board, String word) {
    for (int i = 0; i < board.length; i++)
        for (int j = 0; j < board[0].length; j++)
            if (dfs(board, word, i, j, 0)) return true;
    return false;
}

private boolean dfs(char[][] board, String word, int i, int j, int k) {
    if (k == word.length()) return true;
    if (i < 0 || i >= board.length || j < 0 || j >= board[0].length || board[i][j] != word.charAt(k))
        return false;
    char tmp = board[i][j];
    board[i][j] = '#'; // 标记已访问
    boolean found = dfs(board, word, i + 1, j, k + 1) || dfs(board, word, i - 1, j, k + 1)
                 || dfs(board, word, i, j + 1, k + 1) || dfs(board, word, i, j - 1, k + 1);
    board[i][j] = tmp; // 回溯
    return found;
}
```

- 时间 O(m·n·3^L)，L 为单词长度
- 技巧：原地修改代替 visited 数组

**📝 解析**

- **思路推导**：从每个格子出发 DFS，匹配单词的每个字符。四个方向递归，已访问的格子标记为 '#' 防止重复。
- **关键点**：原地修改 `board[i][j] = '#'` 代替 visited 数组，节省空间。回溯时恢复原值。3^L 是因为每步最多 3 个方向（不能回头）。
- **易错点**：found 为 true 时要短路返回，不要继续搜索其他方向。
- **举一反三**：单词搜索 II（#212）→ 多个单词同时搜索，用 Trie 优化

---

## 7. 分割回文串（#131）

```java
public List<List<String>> partition(String s) {
    List<List<String>> res = new ArrayList<>();
    backtrack(s, 0, new ArrayList<>(), res);
    return res;
}

private void backtrack(String s, int start, List<String> path, List<List<String>> res) {
    if (start == s.length()) { res.add(new ArrayList<>(path)); return; }
    for (int end = start; end < s.length(); end++) {
        if (isPalindrome(s, start, end)) {
            path.add(s.substring(start, end + 1));
            backtrack(s, end + 1, path, res);
            path.remove(path.size() - 1);
        }
    }
}

private boolean isPalindrome(String s, int l, int r) {
    while (l < r) if (s.charAt(l++) != s.charAt(r--)) return false;
    return true;
}
```

- 可用 DP 预处理回文判断优化到 O(1)

**📝 解析**

- **思路推导**：枚举所有分割点，每段是回文才继续。回溯框架：start 表示当前分割起点，end 从 start 到末尾枚举分割终点。
- **关键点**：先判断 `[start, end]` 是否回文，是才递归。可以用 `boolean[][] dp` 预处理所有子串的回文性，将判断从 O(n) 降到 O(1)。
- **易错点**：`s.substring(start, end+1)` 注意 Java substring 是左闭右开。
- **举一反三**：分割回文串 II（#132）→ 求最少分割次数，用 DP

---

## 8. N 皇后（#51）

```java
public List<List<String>> solveNQueens(int n) {
    List<List<String>> res = new ArrayList<>();
    backtrack(n, 0, new int[n], new boolean[n], new boolean[2 * n], new boolean[2 * n], res);
    return res;
}

private void backtrack(int n, int row, int[] queens, boolean[] cols, boolean[] diag1, boolean[] diag2, List<List<String>> res) {
    if (row == n) {
        List<String> board = new ArrayList<>();
        for (int q : queens) {
            char[] line = new char[n];
            Arrays.fill(line, '.');
            line[q] = 'Q';
            board.add(new String(line));
        }
        res.add(board);
        return;
    }
    for (int col = 0; col < n; col++) {
        if (cols[col] || diag1[row - col + n] || diag2[row + col]) continue;
        queens[row] = col;
        cols[col] = diag1[row - col + n] = diag2[row + col] = true;
        backtrack(n, row + 1, queens, cols, diag1, diag2, res);
        cols[col] = diag1[row - col + n] = diag2[row + col] = false;
    }
}
```

- 时间 O(n!)，空间 O(n)
- 用三个布尔数组标记列、主对角线、副对角线

**📝 解析**

- **思路推导**：逐行放置皇后，每行选一列。用三个数组快速判断列、主对角线（row-col+n）、副对角线（row+col）是否冲突。
- **关键点**：主对角线上 row-col 相同，副对角线上 row+col 相同。`row-col` 可能为负，加 n 偏移。
- **易错点**：生成棋盘字符串时用 `Arrays.fill(line, '.')` 初始化，别忘了。
- **举一反三**：N 皇后 II（#52）→ 只要计数不要方案，更简单；数独（#37）→ 9×9 固定大小的回溯
