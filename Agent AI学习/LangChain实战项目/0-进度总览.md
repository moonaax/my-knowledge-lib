# 项目进度总览

> 持续更新，记录每个阶段用到的知识点和对应的学习文档。

## 当前项目能力盘点（截至第四阶段）

````
已具备                              缺失（面试有区分度的能力）
─────────────────────────────────────────────────────────────
✅ LLM 接入（DeepSeek）              ❌ 多 Agent 协作
✅ 多轮对话（InMemory + MemorySaver） ❌ 记忆持久化（重启就忘，MemorySaver 仅内存）
✅ 工具调用（@tool + ToolNode）       ❌ RAG 效果评估（无量化指标）
✅ RAG 检索（FAISS + bge-zh）        ❌ 可观测性（无链路追踪）
✅ LangGraph 状态图（手动构建 ReAct） ❌ 生产化部署（无 Docker）
✅ 自纠错循环（工具失败自动重试换策略）
✅ Plan-and-Execute（先规划再执行，动态调整计划）
✅ 流式输出（SSE + astream_events）
✅ Electron 桌面客户端
````

**现状定位**：能调工具的 Agent，具备 LangGraph 图编排、自纠错、Plan-and-Execute 三种执行模式。

---

## 知识覆盖度

````
                        已覆盖    未覆盖
LangChain 专题
├── 1-学习路线           ✅ 已读
├── 2-Model IO          ████████  ChatModel、Prompt、Memory
├── 3-LCEL              ██████░░  管道、流式、astream_events（并行/分支/重试待学）
├── 4-Tool与Agent       ██████░░  @tool、create_tool_calling_agent、AgentExecutor（Callback自定义/LangSmith待学）
├── 5-RAG实战           ██████░░  Loader、Splitter、Embedding、FAISS、Retriever（MultiQuery/Compression/Ensemble待学）
├── 6-LangGraph编排     ██████░░  StateGraph、ToolNode、条件路由、MemorySaver、自纠错循环（Plan-and-Execute/Multi-Agent待学）
└── 7-生产化部署         ██░░░░░░  FastAPI+SSE（LangSmith/缓存/安全待学）
````

---

## 下一步规划

### 客户端优化（进行中）

| 要做的事 | 状态 | 说明 |
|---------|------|------|
| ToolCard UI 重做 | ✅ | 毛玻璃卡片、状态动画、可折叠 |
| 多会话隔离 | ✅ | messagesMap 按 session_id 存储 |
| 表格渲染修复 | 🟡 | 支持 \|\|/\|\| 分隔符、inline code 保护，边界 case 待验证 |
| Clear 逻辑修复 | ✅ | 只清当前活跃会话 |
| 自纠错 RetryCard | ✅ | 工具失败时显示重试提示卡片（橙色毛玻璃风格） |

### 第四阶段（进阶）：自纠错 + 规划（优先级最高 ⭐⭐⭐）

> 在现有 LangGraph ReAct 基础上增加自纠错和规划能力
> 对应文档：[[../LangChain专题/6-LangGraph编排]]

| 要做的事 | 涉及知识点 | 说明 |
|---------|----------|------|
| 实现自纠错循环 | 循环边、重试逻辑 | 工具调用失败时自动换策略 ✅ |
| 实现 Plan-and-Execute | 多节点图、状态管理 | 先规划再执行，提高复杂任务成功率 ✅ |
| 添加人机协作节点 | interrupt_before | 关键操作前暂停等人确认 |

### 第五阶段：记忆持久化 + RAG 优化（优先级中 ⭐⭐）

| 要做的事 | 涉及知识点 | 说明 |
|---------|----------|------|
| 记忆换成 SQLite | SQLChatMessageHistory | 重启不丢失，改动很小 |
| 混合检索 | EnsembleRetriever（向量+BM25） | 提高检索召回率 |
| 构建评测集 | 手动标注 20-30 个 QA 对 | 量化 RAG 准确率，面试能说数据 |
| 前端 Markdown 渲染 | marked.js + 预处理 | 解决 DeepSeek 非标准格式问题 |

### 第六阶段：生产化部署（优先级中 ⭐⭐）

> 对应文档：[[../LangChain专题/7-生产化部署]]

| 要做的事 | 涉及知识点 | 说明 |
|---------|----------|------|
| Docker 容器化 | Dockerfile + docker-compose | 一键部署 |
| LangSmith 链路追踪 | LANGCHAIN_TRACING_V2 | 可观测性，调试利器 |
| 成本统计 | Callback + token 计数 | 面试能说成本控制方案 |
| API 认证 | FastAPI 中间件 | 安全基础 |

---

## 简历呈现建议

项目名改为有业务场景的：**"基于 LangGraph 的技术知识库智能问答系统"**

````
技术栈：LangChain + LangGraph + DeepSeek + FAISS + FastAPI + Electron
已实现亮点：
1. LangGraph 状态图编排，手动构建 ReAct 循环（StateGraph + ToolNode + 条件路由）
2. 4 个自定义工具，Agent 自主决策调用
3. SSE 流式输出 + astream_events 细粒度事件推送
4. MemorySaver 会话持久化，支持多轮对话
5. 自纠错循环（工具失败自动重试换策略，扩展 AgentState + corrector 节点）
6. Plan-and-Execute（planner 规划 → executor 执行 → replanner 动态调整）
7. 前端 RetryCard / PlanCard 组件，实时展示执行过程
待实现亮点（面试加分项）：
8. 人机协作节点（interrupt_before）
9. SQLite 持久化记忆 / LangSmith 全链路追踪
````
