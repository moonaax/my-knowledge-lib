# 项目知识点分析

> 持续更新，记录每个阶段用到的知识点和对应的学习文档。

## 第一阶段：多轮对话客户端

### 1. 终端版 (`chat.py`)

| 代码 | 知识点 | 对应文档 |
|------|--------|---------|
| `ChatOpenAI(base_url=..., model=...)` | Model I/O — 模型初始化，DeepSeek 兼容 OpenAI 接口 | [[../LangChain专题/2-Model IO详解]] |
| `ChatPromptTemplate.from_messages()` | Prompt Template — 系统提示 + 用户输入模板 | [[../LangChain专题/2-Model IO详解]] |
| `MessagesPlaceholder("history")` | 动态消息占位符 — 运行时插入历史对话 | [[../LangChain专题/2-Model IO详解]] |
| `prompt \| llm` | LCEL 管道符 — 把 prompt 和 llm 串成 Chain | [[../LangChain专题/3-LCEL表达式语言]] |
| `RunnableWithMessageHistory` | Memory 记忆管理 — 自动存取对话历史 | [[../LangChain专题/2-Model IO详解]] |
| `InMemoryChatMessageHistory` | 会话存储 — 内存中保存消息列表 | [[../LangChain专题/2-Model IO详解]] |
| `chain.stream(...)` | 流式输出 — 逐 chunk 返回 | [[../LangChain专题/3-LCEL表达式语言]] |
| `python-dotenv` + `.env` | 环境变量管理 — API Key 不硬编码 | 工程实践 |

### 2. FastAPI 后端 (`server.py`)

| 代码 | 知识点 | 对应文档 |
|------|--------|---------|
| `FastAPI()` + `@app.post("/chat")` | REST API — 把 Chain 暴露为 HTTP 接口 | [[../LangChain专题/7-生产化部署]] |
| `StreamingResponse` + SSE | Server-Sent Events — `data: xxx\n\n` 推送流式数据 | [[../LangChain专题/7-生产化部署]] |
| `chain.astream(...)` | 异步流式调用 — `async for` 配合 FastAPI | [[../LangChain专题/3-LCEL表达式语言]] |
| `CORSMiddleware` | 跨域处理 — Electron 请求 localhost | Web 工程基础 |
| Pydantic `BaseModel` | 请求体校验 — 自动验证 JSON 参数 | FastAPI 基础 |

### 3. Electron 桌面客户端 (`electron/`)

| 文件 | 知识点 | 说明 |
|------|--------|------|
| `main.js` — `BrowserWindow` | Electron 主进程 — 创建窗口、加载 HTML | Electron 基础 |
| `titleBarStyle: 'hiddenInset'` | macOS 原生窗口 — 隐藏标题栏保留红绿灯 | Electron macOS 适配 |
| `package.json` | Node.js 项目管理 — 依赖和启动脚本 | Node.js 基础 |

### 4. 前端界面 (`index.html`)

| 功能 | 知识点 |
|------|--------|
| `fetch()` + `ReadableStream` | Fetch API 流式读取 — 逐块解析 SSE |
| `data-theme` + CSS 变量 | 主题系统 — 深浅色切换，`localStorage` 持久化 |
| `backdrop-filter: blur()` | 毛玻璃效果 |
| `radial-gradient` + `animation` | 环境光晕 — 渐变球体漂移动画 |
| `mask` + `linear-gradient` | 渐变发光边框 — mask-composite 技巧 |
| `@keyframes msgIn` | 消息入场动画 — 淡入 + 上移 |
| `compositionstart/end` | 输入法兼容 — 防止 IME 回车误触发 |
| `text-overflow: ellipsis` | 文本溢出省略 |
| `-webkit-app-region: drag` | Electron 窗口拖拽区域 |

### 5. 工程化

| 操作 | 知识点 |
|------|--------|
| `start.sh` | Shell 脚本 — 后台启动后端 + 前台启动 Electron |
| `.env` / `.env.example` | 密钥管理 — 模板 + gitignore |
| Git 提交管理 | 版本控制 — feat/fix/docs 提交规范 |

---

## 知识覆盖度

````
                        已覆盖    未覆盖
LangChain 专题
├── 1-学习路线           ✅ 已读
├── 2-Model IO          ████████  ChatModel、Prompt、Memory
├── 3-LCEL              ██████░░  管道、流式、astream_events（并行/分支/重试待学）
├── 4-Tool与Agent       ██████░░  @tool、create_tool_calling_agent、AgentExecutor（Callback自定义/LangSmith待学）
├── 5-RAG实战           ░░░░░░░░  第三阶段
├── 6-LangGraph编排     ░░░░░░░░  第四阶段
└── 7-生产化部署         ██░░░░░░  FastAPI+SSE（LangSmith/缓存/安全待学）
````
---

## 第二阶段：工具调用 Agent

> 日期：2026-04-21 | 代码：`~/lib/langchain-deepseek-chat/`

### 1. 工具定义 (`chat.py` / `server.py`)

| 代码 | 知识点 | 对应文档 |
|------|--------|---------|
| `@tool` 装饰器 | Tool — 最简工具定义方式，自动提取 name/description/args_schema | [[../LangChain专题/4-Tool与Agent]] |
| `calculator` 工具 | 安全沙箱 — `eval` + `{"__builtins__": {}}` 限制执行环境 | [[../LangChain专题/4-Tool与Agent]] |
| 工具 docstring | 工具描述 — LLM 完全依赖描述决定何时使用，质量直接影响 Agent 表现 | [[../LangChain专题/4-Tool与Agent]] |
| `tools = [...]` | 工具列表 — 控制在 3-8 个，太多会降低选择准确率 | [[../LangChain专题/4-Tool与Agent]] |

### 2. Agent 构建 (`chat.py` / `server.py`)

| 代码 | 知识点 | 对应文档 |
|------|--------|---------|
| `MessagesPlaceholder("agent_scratchpad")` | Agent Prompt — 与普通 Chain 的关键区别，存放中间推理过程 | [[../LangChain专题/4-Tool与Agent]] |
| `create_tool_calling_agent(llm, tools, prompt)` | Function Calling Agent — 基于 LLM 原生能力，比 ReAct 更准确 | [[../LangChain专题/4-Tool与Agent]] |
| `AgentExecutor(agent, tools, ...)` | 推理循环引擎 — 执行 Thought→Action→Observation 循环 | [[../LangChain专题/4-Tool与Agent]] |
| `max_iterations=10` | 安全边界 — 防止 Agent 无限循环 | [[../LangChain专题/4-Tool与Agent]] |
| `handle_parsing_errors=True` | 错误恢复 — 自动处理 LLM 输出格式错误 | [[../LangChain专题/4-Tool与Agent]] |
| `RunnableWithMessageHistory` 包装 AgentExecutor | Memory + Agent — 记忆机制同样适用于 Agent | [[../LangChain专题/2-Model IO详解]] |

### 3. Agent API 流式推送 (`server.py`)

| 代码 | 知识点 | 对应文档 |
|------|--------|---------|
| `astream_events(version="v2")` | 细粒度事件流 — 比 astream 更详细，能捕获工具调用过程 | [[../LangChain专题/3-LCEL表达式语言]] |
| `on_tool_start` / `on_tool_end` | 工具事件 — 获取工具名、输入参数、返回结果 | [[../LangChain专题/4-Tool与Agent]] |
| `on_chat_model_stream` | LLM token 事件 — 最终回答的逐字输出 | [[../LangChain专题/3-LCEL表达式语言]] |
| `event: tool_start\ndata: {...}\n\n` | SSE 自定义事件类型 — 标准 SSE 支持 event 字段区分消息类型 | [[../LangChain专题/7-生产化部署]] |

### 4. 前端工具调用展示 (`index.html`)

| 功能 | 知识点 |
|------|--------|
| SSE 多事件类型解析 | 按 `\n\n` 分割消息，解析 `event:` 和 `data:` 字段 |
| `.tool-call` 卡片 | 工具调用可视化 — pending/done 状态切换 |
| 文本节点管理 | DOM 操作 — 工具卡片后追加文本需要新建 TextNode |
| `escHtml()` | XSS 防护 — 转义工具返回的 HTML 特殊字符 |

### 5. 架构变化

````
第一阶段: prompt | llm → RunnableWithMessageHistory → stream/astream
第二阶段: create_tool_calling_agent → AgentExecutor → RunnableWithMessageHistory → astream_events
````

---

## 第三阶段：知识库 RAG 问答

> 🔲 待开始

---

## 第四阶段：LangGraph 多步骤 Agent

> 🔲 待开始

---

## 第五阶段：生产化部署

> 🔲 待开始
